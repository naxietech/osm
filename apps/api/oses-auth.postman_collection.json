{
  "info": {
    "name": "OSES — Auth API (Phase 1)",
    "description": "Backend auth + user-management endpoints.\n\nAuth is COOKIE-BASED: after `Login`, Postman's cookie jar stores `oses_access` + `oses_refresh` (HttpOnly) and sends them automatically on later requests — so `Me`, `Refresh`, admin routes, etc. just work. There are no tokens in the response body.\n\nSet the `baseUrl` variable if your API isn't on http://localhost:3001. The user-management routes need a Super Admin session (they require the `users.manage` grant).",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    { "key": "baseUrl", "value": "http://localhost:3001/api/v1" },
    { "key": "userId", "value": "" }
  ],
  "item": [
    {
      "name": "Auth",
      "item": [
        {
          "name": "Login",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "url": "{{baseUrl}}/auth/login",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"superadmin@oses.pk\",\n  \"password\": \"change-me-to-a-strong-password-min-12-chars\"\n}"
            },
            "description": "Verify email + password; sets the HttpOnly auth cookies. Wrong password (6+ chars) → 401; short password → 400; 5 wrong attempts → account locked ~15 min."
          }
        },
        {
          "name": "Me",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{baseUrl}}/auth/me",
            "description": "Current user (SafeUser). Uses the access cookie. 401 without a session."
          }
        },
        {
          "name": "Permissions",
          "request": {
            "method": "GET",
            "header": [],
            "url": "{{baseUrl}}/auth/permissions",
            "description": "The caller's permission grants (action + scope)."
          }
        },
        {
          "name": "Refresh",
          "request": {
            "method": "POST",
            "header": [],
            "url": "{{baseUrl}}/auth/refresh",
            "description": "Rotate the refresh token and reissue cookies. Replaying an old refresh token → 401 (reuse detected, family revoked)."
          }
        },
        {
          "name": "Change password",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "url": "{{baseUrl}}/auth/password/change",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"currentPassword\": \"current-password\",\n  \"newPassword\": \"new-strong-password\"\n}"
            },
            "description": "Change your own password. Revokes ALL sessions — you must log in again afterward."
          }
        },
        {
          "name": "Logout",
          "request": {
            "method": "POST",
            "header": [],
            "url": "{{baseUrl}}/auth/logout",
            "description": "Revoke the current session and clear cookies."
          }
        }
      ]
    },
    {
      "name": "Users (Super Admin only)",
      "description": "Require an authenticated session with the users.manage grant (Super Admin). Others get 403.",
      "item": [
        {
          "name": "Create user",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "url": "{{baseUrl}}/users",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"staff@oses.pk\",\n  \"fullName\": \"Staff Member\",\n  \"roleId\": \"role_admin\",\n  \"password\": \"temporary-password-123\"\n}"
            },
            "description": "Create a user with a role + temporary password. roleId is one of: role_super_admin, role_admin, role_controller, role_checker, role_institute. Duplicate email → 409. Not a Super Admin → 403."
          }
        },
        {
          "name": "Reset password (admin)",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "url": "{{baseUrl}}/users/:userId/reset-password",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"password\": \"new-temporary-password-123\"\n}"
            },
            "description": "Set a new temporary password for a user (replaces the emailed reset flow). Revokes their sessions. Put the user id in the :userId path variable."
          }
        },
        {
          "name": "Set status (activate / suspend)",
          "request": {
            "method": "PATCH",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "url": "{{baseUrl}}/users/:userId/status",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"status\": \"suspended\"\n}"
            },
            "description": "Activate or suspend an account. status is 'active' or 'suspended'. Suspending revokes the user's sessions immediately."
          }
        }
      ]
    }
  ]
}
